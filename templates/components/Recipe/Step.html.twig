<div class="flex flex-col gap-3">
    {% for step in recipeSteps %}
        <p class="flex gap-1">
            <span>{{ step.step }}. {{ step.description }}</span>
        </p>
    {% endfor %}
</div>