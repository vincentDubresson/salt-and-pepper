<div class="flex items-center overflow-hidden w-full mb-5">
    <a href="{{ path('app_home') }}" class="inline-flex items-center text-xs text-gray-500">
        Accueil
        <twig:ux:icon name="lucide:chevron-right" class="w-3 h-3 mx-1 text-primary-500" />
    </a>

    {# Todo : Ajouter lien vers catégories #}
    <a href="#" class="flex items-center text-xs font-medium text-gray-500">
        {{ subcategory.category.label }}
        <twig:ux:icon name="lucide:chevron-right" class="w-3 h-3 mx-1 text-primary-500" />
    </a>

    {# Todo : Ajouter lien vers sous-catégories #}
    <a href="#" class="flex items-center text-xs font-medium text-gray-500">
        {{ subcategory.label }}
        <twig:ux:icon name="lucide:chevron-right" class="w-3 h-3 mx-1 text-primary-500" />
    </a>

    <p class="flex items-center truncate text-xs font-medium text-gray-500">
        <span class="truncate">{{ recipeTitle }}</span>
    </p>
</div>
